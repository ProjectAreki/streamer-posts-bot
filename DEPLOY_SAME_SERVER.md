# 🖥️ Размещение на том же сервере

## ✅ ДА, можно разместить на том же сервере!

**Оба бота могут работать параллельно на одном сервере без конфликтов.**

---

## 📊 Требования к ресурсам

### Размер проекта:

```
Код проекта:        ~500 KB (без venv)
С зависимостями:    ~50-100 MB (venv)
Логи:               ~10-50 MB/месяц (зависит от активности)
Session файлы:      ~1-5 MB (Telethon)
─────────────────────────────────────
ИТОГО:              ~100-200 MB на диске
```

### Память (RAM):

```
Основной бот:       ~100-200 MB
StreamerPostsBot:   ~50-100 MB
─────────────────────────────────────
ИТОГО:              ~150-300 MB RAM
```

### CPU:

- Оба бота работают асинхронно
- Нагрузка минимальная (только при генерации постов)
- Можно запускать на одном ядре

---

## 🎯 Преимущества размещения на одном сервере

✅ **Экономия ресурсов** - один сервер вместо двух
✅ **Простое управление** - все в одном месте
✅ **Общие зависимости** - можно использовать один venv (опционально)
✅ **Единые логи** - проще отслеживать
✅ **Нет конфликтов** - разные порты, разные токены

---

## 📁 Рекомендуемая структура на сервере

```
/home/user/bots/
├── main_bot/              # Основной бот
│   ├── bot.py
│   ├── src/
│   ├── venv/
│   └── .env
│
└── streamer_posts_bot/     # Новый бот
    ├── bot.py
    ├── src/
    ├── venv/              # Отдельный venv (рекомендуется)
    └── .env
```

**Или в одной папке:**

```
/home/user/bots/
├── main_bot/
│   └── ...
└── streamer_posts_bot/
    └── ...
```

---

## 🚀 Варианты деплоя

### Вариант 1: Отдельные venv (рекомендуется)

**Преимущества:**
- ✅ Полная изоляция зависимостей
- ✅ Можно использовать разные версии библиотек
- ✅ Легче обновлять независимо

**Команды:**

```bash
# 1. Скопировать проект на сервер
scp -r streamer_posts_bot user@server:/home/user/bots/

# 2. Подключиться к серверу
ssh user@server

# 3. Перейти в директорию
cd /home/user/bots/streamer_posts_bot

# 4. Создать venv и установить зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 5. Настроить .env
cp .env.example .env
nano .env  # Отредактировать

# 6. Запустить
python bot.py
```

---

### Вариант 2: Общий venv (экономия места)

**Преимущества:**
- ✅ Экономия места (~50 MB)
- ✅ Быстрее установка (зависимости уже есть)

**Недостатки:**
- ⚠️ Конфликты версий возможны
- ⚠️ Сложнее управлять зависимостями

**Команды:**

```bash
# 1. Скопировать проект
scp -r streamer_posts_bot user@server:/home/user/bots/

# 2. Подключиться
ssh user@server

# 3. Перейти в директорию основного бота
cd /home/user/bots/main_bot

# 4. Активировать существующий venv
source venv/bin/activate

# 5. Установить недостающие зависимости (если есть)
cd ../streamer_posts_bot
pip install -r requirements.txt

# 6. Настроить .env
cp .env.example .env
nano .env

# 7. Запустить (venv уже активирован)
python bot.py
```

---

## ⚙️ Systemd сервисы (автозапуск)

### Создать два отдельных сервиса:

#### 1. Основной бот: `/etc/systemd/system/main-bot.service`

```ini
[Unit]
Description=Main Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/home/user/bots/main_bot
Environment="PATH=/home/user/bots/main_bot/venv/bin"
ExecStart=/home/user/bots/main_bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

#### 2. StreamerPostsBot: `/etc/systemd/system/streamer-bot.service`

```ini
[Unit]
Description=Streamer Posts Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/home/user/bots/streamer_posts_bot
Environment="PATH=/home/user/bots/streamer_posts_bot/venv/bin"
ExecStart=/home/user/bots/streamer_posts_bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Управление сервисами:

```bash
# Перезагрузить конфигурацию
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable main-bot
sudo systemctl enable streamer-bot

# Запустить
sudo systemctl start main-bot
sudo systemctl start streamer-bot

# Проверить статус
sudo systemctl status main-bot
sudo systemctl status streamer-bot

# Просмотр логов
sudo journalctl -u main-bot -f
sudo journalctl -u streamer-bot -f
```

---

## 🔍 Проверка совместимости

### Что НЕ конфликтует:

✅ **Разные токены ботов** - каждый бот использует свой токен
✅ **Разные порты** - не используются (polling через Telegram API)
✅ **Разные session файлы** - Telethon создаёт отдельные `.session` файлы
✅ **Разные логи** - каждый бот пишет в свою папку `logs/`

### Что может конфликтовать:

⚠️ **Telethon session** - если используете один аккаунт для обоих ботов
   - **Решение:** Используйте разные session файлы или разные аккаунты

⚠️ **OpenRouter API ключ** - если используете один ключ
   - **Решение:** Можно использовать один ключ (лимиты общие)

---

## 💾 Проверка места на диске

```bash
# Проверить свободное место
df -h

# Проверить размер проектов
du -sh /home/user/bots/main_bot
du -sh /home/user/bots/streamer_posts_bot

# Проверить размер venv
du -sh /home/user/bots/*/venv
```

**Минимальные требования:**
- Свободное место: **500 MB** (с запасом)
- Рекомендуется: **1 GB** (для логов и роста)

---

## 📊 Мониторинг ресурсов

### Проверка использования памяти:

```bash
# Все процессы Python
ps aux | grep python

# Память по процессам
ps aux | grep python | awk '{sum+=$6} END {print sum/1024 " MB"}'
```

### Проверка CPU:

```bash
# Нагрузка на CPU
top -p $(pgrep -f "python.*bot.py")
```

---

## 🛠️ Быстрый деплой (скрипт)

Создайте файл `deploy_to_server.sh`:

```bash
#!/bin/bash
# Деплой StreamerPostsBot на существующий сервер

SERVER="user@your-server.com"
REMOTE_PATH="/home/user/bots"
LOCAL_PATH="./streamer_posts_bot"

echo "📦 Копирование проекта на сервер..."
scp -r $LOCAL_PATH $SERVER:$REMOTE_PATH/

echo "🔧 Настройка на сервере..."
ssh $SERVER << 'EOF'
cd /home/user/bots/streamer_posts_bot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
echo "⚠️  Не забудьте отредактировать .env файл!"
EOF

echo "✅ Деплой завершён!"
echo "📝 Следующие шаги:"
echo "   1. ssh $SERVER"
echo "   2. cd $REMOTE_PATH/streamer_posts_bot"
echo "   3. nano .env  # Настроить конфигурацию"
echo "   4. source venv/bin/activate"
echo "   5. python bot.py"
```

---

## ✅ Чеклист деплоя

- [ ] Скопировать проект на сервер
- [ ] Создать venv (или использовать существующий)
- [ ] Установить зависимости
- [ ] Настроить `.env` файл
- [ ] Проверить свободное место на диске
- [ ] Протестировать запуск
- [ ] Настроить systemd сервис (опционально)
- [ ] Проверить логи

---

## 🎯 Итог

**✅ Да, можно разместить на том же сервере!**

**Требования:**
- Место на диске: ~100-200 MB
- RAM: ~50-100 MB дополнительно
- CPU: минимальная нагрузка

**Рекомендации:**
- Использовать отдельные venv для изоляции
- Создать отдельные systemd сервисы
- Мониторить использование ресурсов

**Оба бота будут работать параллельно без конфликтов!** 🚀

---

**Создано:** 15 января 2026
