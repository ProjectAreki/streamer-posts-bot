# 🚀 Быстрый старт: Обучение AI на твоих постах

## Шаг 1: Подготовь JSON с твоими 500 постами

Создай файл `data/my_posts.json`:

```json
{
  "posts": [
    {"text": "твой пост 1..."},
    {"text": "твой пост 2..."},
    ...
    {"text": "твой пост 500..."}
  ]
}
```

Или простой массив:
```json
[
  "твой пост 1...",
  "твой пост 2...",
  ...
]
```

## Шаг 2: Добавь в бота

В файле `bot.py` или `src/handlers/streamer_posts_handlers.py`, перед генерацией постов:

```python
# Загрузка существующих постов
generator.load_existing_posts_from_file("data/my_posts.json")
```

## Шаг 3: Всё!

Теперь при генерации:
- ✅ AI будет учиться на твоих постах
- ✅ Каждый пост получит 3 примера из твоей базы
- ✅ Проверка уникальности сравнит с твоими 500 постами

## Где добавить код?

Найди в `src/handlers/streamer_posts_handlers.py` место где создается `AIPostGenerator`:

```python
generator = AIPostGenerator(
    openrouter_api_key=config.openrouter.api_key,
    model=config.openrouter.default_model,
    use_openrouter=True
)

# ← ДОБАВЬ ЗДЕСЬ:
generator.load_existing_posts_from_file("data/my_posts.json")

# Дальше продолжается генерация постов...
```

## Проверка

В логах увидишь:
```
✅ Загружено 500 существующих постов для обучения AI
```

Готово! AI теперь обучен на твоих постах.
