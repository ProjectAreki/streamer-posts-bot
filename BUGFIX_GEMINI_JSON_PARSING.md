# Исправление ошибки парсинга JSON в Gemini 2.0 Flash

## Проблема

При проверке уникальности постов через Gemini 2.0 Flash возникала ошибка:

```
⚠️ Ошибка проверки:
Ошибка парсинга JSON: Unterminated string starting at: line 232 column 7 (char 5262). 
AI вернул невалидный JSON.

Модель: Gemini 2.0 Flash
```

### Причина

Gemini 2.0 Flash обрывал JSON-ответ на середине строки из-за недостаточного лимита токенов (было 2000 токенов).

## Исправления

### 1. Увеличен лимит токенов (src/ai_post_generator.py)

**До:**
```python
"max_tokens": 2000
```

**После:**
```python
"max_tokens": 4000  # Увеличено до 4000 чтобы JSON не обрывался
```

### 2. Добавлена умная фиксация оборванных JSON

Если JSON всё равно обрывается, система теперь:
- Определяет позицию ошибки
- Убирает незавершенную строку
- Автоматически закрывает все открытые скобки `{}` и `[]`
- Добавляет предупреждение: "⚠️ JSON был оборван, автоматически исправлено"

### 3. Улучшено логирование

- Увеличен размер raw_response с 500 до 1000 символов
- Добавлены детали ошибки:
  - Тип ошибки
  - Сообщение об ошибке
  - Длина оригинального ответа

## Поиск пропущенных постов

Создан скрипт `find_missing_posts.py` для анализа последних сессий генерации:

### Использование

```bash
python find_missing_posts.py
```

Скрипт покажет:
- Сколько постов было создано в последней сессии
- Сколько постов пропущено
- Какие слоты использовались
- Диапазон ID созданных постов
- Рекомендации по исправлению

### Пример вывода

```
============================================================
ПОСЛЕДНЯЯ СЕССИЯ ГЕНЕРАЦИИ
============================================================
Дата: 2026-01-18 17:32:15
Постов создано: 75
Ожидалось: 80

[!] ПРОПУЩЕНО ПОСТОВ: 5

Использованные слоты:
   - Gates of Olympus: 8 постов
   - Sweet Bonanza: 7 постов
   - Dog House: 6 постов
   ...

Диапазон ID созданных постов:
   От #420 до #494

РЕКОМЕНДАЦИИ:
   1. Проверьте логи бота на наличие ошибок
   2. Посмотрите последние 5 видео в исходном канале
   3. Возможно, некоторые видео не прошли валидацию
   4. Запустите генерацию недостающих 5 постов
```

## Текущее состояние базы данных

- Всего постов в базе: **501**
- Последний пост: ID 501 от 2026-01-18 19:24
- Последняя полная сессия: 18 января (80 постов)

## Рекомендации

1. **Для проверки уникальности** теперь рекомендуется:
   - ⚡ **Быстрая** (Gemini Flash) - исправлена, работает стабильно
   - 👍 **Хорошая** (GPT-4o Mini) - альтернатива, если нужна надежность
   - 🔄 **Гибридная** (Flash + Gemini 3 Pro) - лучший баланс

2. **При возникновении ошибок**:
   - Проверьте размер raw_response в логах
   - Если JSON всё равно обрывается, используйте другую модель (GPT-4o Mini или Gemini 3 Pro)
   - Свяжитесь с разработчиком для дальнейшей диагностики

3. **Мониторинг пропущенных постов**:
   - Запускайте `find_missing_posts.py` после каждой большой сессии генерации
   - Проверяйте логи бота на наличие ошибок
   - Обращайте внимание на валидацию данных из видео

## Технические детали

### Структура обработки JSON

1. Убираем markdown обертки (```json, ```)
2. Убираем комментарии //
3. Убираем trailing commas
4. Пытаемся парсить JSON
5. Если ошибка "Unterminated string":
   - Находим позицию ошибки
   - Обрезаем до последней валидной позиции
   - Закрываем все открытые скобки
   - Добавляем warning
6. Если всё равно не парсится:
   - Ищем JSON через regex
   - Если не находим - возвращаем default response

### Fallback поведение

Если проверка уникальности не может быть выполнена из-за ошибок парсинга:
- Посты считаются **уникальными по умолчанию**
- Возвращается warning с деталями ошибки
- Процесс генерации **продолжается**

Это гарантирует, что даже при сбое проверки пользователь может продолжить работу.

## Дата исправления

21 января 2026

## Статус

✅ **Исправлено и протестировано**
