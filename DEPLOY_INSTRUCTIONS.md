# Инструкция по деплою на сервер

## ✅ Git Push выполнен успешно!

Изменения отправлены в репозиторий:
- Commit: `57eb7df`
- Изменения: Исправление ошибки парсинга JSON в Gemini 2.0 Flash

## 🚀 Деплой на сервер

### Вариант 1: Через SSH

1. **Подключитесь к серверу:**
```bash
ssh user@your-server.com
```

2. **Перейдите в директорию проекта:**
```bash
cd /path/to/streamer_posts_bot
```

3. **Обновите код с GitHub:**
```bash
git pull origin main
```

4. **Запустите deploy скрипт:**
```bash
bash deploy.sh
```

5. **Перезапустите бота:**
```bash
# Если бот запущен в systemd:
sudo systemctl restart streamer-posts-bot

# Если бот запущен через nohup:
pkill -f bot.py
nohup python bot.py > logs/bot.log 2>&1 &

# Если бот запущен в screen/tmux:
# Зайдите в сессию и перезапустите
```

### Вариант 2: Автоматический deploy (если настроен)

Если у вас настроен GitHub Actions или webhook:
1. Изменения автоматически подтянутся на сервер
2. Бот перезапустится автоматически

### Вариант 3: Ручное обновление файлов

Если нет доступа к git на сервере:

1. **Скопируйте измененные файлы на сервер:**
```bash
scp src/ai_post_generator.py user@server:/path/to/streamer_posts_bot/src/
scp find_missing_posts.py user@server:/path/to/streamer_posts_bot/
scp BUGFIX_GEMINI_JSON_PARSING.md user@server:/path/to/streamer_posts_bot/
scp QUICK_FIX_GUIDE.md user@server:/path/to/streamer_posts_bot/
```

2. **Перезапустите бота на сервере**

## 📋 Проверка после деплоя

1. **Проверьте что бот запущен:**
```bash
ps aux | grep bot.py
```

2. **Посмотрите логи:**
```bash
tail -f logs/bot.log
```

3. **Проверьте в Telegram:**
   - Откройте бота
   - Попробуйте сценарий "📹 100 постов стримеров"
   - Проверьте что проверка уникальности работает без ошибок

## 🔍 Тестирование исправлений

### 1. Тест проверки уникальности

1. Сгенерируйте несколько постов (5-10)
2. При проверке выберите "⚡ Быстрая (Gemini Flash)"
3. Проверка должна пройти БЕЗ ошибки JSON

### 2. Поиск пропущенных постов

На сервере запустите:
```bash
python find_missing_posts.py
```

Введите:
- Ожидалось постов: `80`
- Период: `1` (1 час, т.к. вы делали посты 10 минут назад)

## 📊 Мониторинг

После деплоя рекомендуется:

1. **Проверить логи на ошибки:**
```bash
tail -n 100 logs/bot.log | grep -i error
```

2. **Проверить статус бота:**
```bash
# Если используется systemd:
sudo systemctl status streamer-posts-bot
```

3. **Протестировать генерацию постов:**
   - Создайте тестовую партию из 5-10 постов
   - Убедитесь что проверка уникальности работает
   - Проверьте что публикация проходит успешно

## 🆘 Если что-то пошло не так

### Бот не запускается

```bash
# Проверьте логи:
cat logs/bot.log

# Проверьте зависимости:
pip list | grep aiogram
pip list | grep telethon

# Переустановите зависимости:
pip install -r requirements.txt --upgrade
```

### Ошибки при проверке уникальности

1. Проверьте что OPENROUTER_API_KEY в .env файле
2. Используйте альтернативную модель:
   - 👍 Хорошая (GPT-4o Mini)
   - ⭐ Отличная (Gemini 3 Pro)

### Не находятся пропущенные посты

Увеличьте период поиска:
```bash
python find_missing_posts.py
# Введите: 168 (7 дней)
```

## ✅ Чеклист деплоя

- [x] Git commit создан
- [x] Git push выполнен
- [ ] Подключение к серверу
- [ ] Git pull на сервере
- [ ] Перезапуск бота
- [ ] Проверка логов
- [ ] Тестирование генерации постов
- [ ] Тестирование проверки уникальности
- [ ] Запуск find_missing_posts.py для постов созданных 10 минут назад

## 💡 Полезные команды

```bash
# Просмотр последних логов
tail -f logs/bot.log

# Поиск ошибок в логах
grep -i "error\|exception" logs/bot.log

# Перезапуск бота (systemd)
sudo systemctl restart streamer-posts-bot

# Проверка статуса
sudo systemctl status streamer-posts-bot

# Проверка процесса
ps aux | grep bot.py

# Убить процесс бота
pkill -f bot.py
```
