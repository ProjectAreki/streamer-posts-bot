# 🧠 Memory MCP Server - Настройка и использование

## 📌 Что это?

**Memory MCP Server** - это сервер Model Context Protocol, который позволяет AI ассистенту сохранять и восстанавливать контекст проекта между сессиями.

## ✅ Что добавлено в проект

### 1. Конфигурация MCP
Файл: `.cursor/mcp_config.json`

```json
{
  "mcpServers": {
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Сохранение контекста проекта между сессиями"
    }
  }
}
```

### 2. Документация
- `.cursor/README.md` - описание всех MCP серверов
- `MCP_MEMORY_SETUP.md` - этот файл

### 3. Обновлён .gitignore
Добавлены правила для MCP файлов:
- Конфигурация коммитится (`.cursor/mcp_config.json`)
- Runtime файлы игнорируются (`.cursor/memory/`, `.cursor/cache/`)

## 🚀 Установка

### Требования:
- **Node.js 16+** и npm
- **Cursor IDE** с поддержкой MCP

### Проверка установки Node.js:
```bash
node --version  # должно быть >= v16.0.0
npm --version   # любая версия
```

### Автоматическая установка:
MCP сервер устанавливается автоматически при первом использовании через `npx`.

### Ручная установка (опционально):
```bash
npm install -g @modelcontextprotocol/server-memory
```

## 📖 Как использовать

### 1. Сохранение важной информации

**Пример 1: Конфигурация проекта**
```
Запомни: в этом проекте используется OpenRouter API с ключом в .env файле.
Основные модели: Qwen 3 235B (дешёвая) и Gemini Flash (оптимальная).
```

**Пример 2: Особенности кода**
```
Запомни: функция generate_posts в ai_post_generator.py использует ротацию
моделей и "Сторожевой AI" для проверки уникальности.
```

**Пример 3: Проблемы и решения**
```
Запомни: если Telethon выдаёт ошибку сессии, нужно удалить .session файл
и переавторизоваться.
```

### 2. Получение сохранённой информации

```
Что ты помнишь о настройках AI в этом проекте?
```

```
Напомни, какие модели мы используем для генерации постов?
```

```
Какие были проблемы с Telethon?
```

### 3. Обновление информации

```
Обнови информацию: теперь используем Claude Sonnet вместо Qwen
```

### 4. Удаление информации

```
Забудь информацию о проблемах с Telethon
```

## 💡 Что сохранять в Memory

### ✅ Полезно сохранять:

1. **Конфигурация проекта:**
   - API ключи (где хранятся, как получить)
   - Настройки окружения
   - Важные переменные .env

2. **Архитектурные решения:**
   - Почему выбрана определённая библиотека
   - Особенности реализации
   - Известные ограничения

3. **Проблемы и решения:**
   - Типичные ошибки и их исправление
   - Workarounds для известных багов
   - Советы по деплою

4. **Специфика бизнес-логики:**
   - Как работает генерация постов
   - Какие модели AI использовать
   - Особенности форматирования

5. **Доступы и учётные данные (ТОЛЬКО метаинформация!):**
   - Где хранятся доступы (но НЕ сами доступы!)
   - Какие сервисы используются
   - Ссылки на документацию

### ❌ НЕ сохранять:

- ❌ Реальные пароли и API ключи
- ❌ Токены доступа
- ❌ Приватные данные пользователей
- ❌ Код целиком (для этого есть кодовая база)

## 🎯 Примеры для Streamer Posts Bot

### Начальная настройка памяти:

```
Запомни следующую информацию о проекте Streamer Posts Bot:

1. Назначение: генерация 100 уникальных постов про стримеров казино
   - 80 постов с видео
   - 20 постов с картинками

2. Технологии:
   - aiogram 3.x для Telegram Bot API
   - telethon 1.34+ для работы с каналами
   - OpenRouter API для AI генерации
   - 15+ AI моделей

3. Ключевые модули:
   - ai_post_generator.py (3547 строк) - AI генерация
   - streamer_posts_handlers.py (2706 строк) - обработчики
   - bonus_generator.py (412 строк) - вариации бонусов

4. Стоимость генерации:
   - Дешёвые модели (Qwen, DeepSeek): ~5₽ за 100 постов
   - Средние (Gemini Flash): ~40₽ за 100 постов
   - Премиум (Claude, GPT): ~100-280₽ за 100 постов

5. Конфигурация:
   - .env файл с: BOT_TOKEN, API_ID, API_HASH, OPENROUTER_KEY
   - Логи в logs/bot.log
   - Сессии Telethon в .session файлах

6. Известные особенности:
   - "Сторожевой AI" проверяет уникальность постов
   - Умные паузы при публикации (3-5 сек, 60 сек каждые 20 постов)
   - Поддержка копирования медиа из каналов через Telethon
```

### Сохранение решений проблем:

```
Запомни решение: если OpenRouter возвращает ошибку 401, нужно:
1. Проверить OPENROUTER_API_KEY в .env
2. Проверить баланс на https://openrouter.ai/
3. Убедиться, что ключ начинается с "sk-or-v1-"
```

```
Запомни: для публикации в канал бот должен быть администратором
с правами на публикацию. Это настраивается в настройках канала.
```

## 🔍 Проверка работы Memory Server

### 1. Сохраните информацию:
```
Запомни: тестовое сообщение для проверки Memory MCP
```

### 2. Перезапустите Cursor IDE

### 3. Спросите:
```
Что ты помнишь о тестовом сообщении?
```

Если Memory работает, AI вспомнит сохранённую информацию.

## 🐛 Troubleshooting

### Memory не работает?

**1. Проверьте Node.js:**
```bash
node --version
```
Должно быть >= v16.0.0

**2. Проверьте конфигурацию:**
```bash
cat .cursor/mcp_config.json
```

**3. Проверьте логи Cursor:**
- Откройте Developer Tools в Cursor
- Посмотрите Console на ошибки MCP

**4. Попробуйте ручную установку:**
```bash
npm install -g @modelcontextprotocol/server-memory
```

**5. Перезапустите Cursor IDE**

### Memory не сохраняется между сессиями?

- Убедитесь, что `.cursor/` папка не удаляется
- Проверьте права доступа к `.cursor/memory/`
- Убедитесь, что Memory Server успешно запускается

## 📊 Структура данных Memory

Memory Server хранит данные в формате:
```
.cursor/
  ├── mcp_config.json      # Конфигурация (коммитится)
  ├── README.md            # Документация (коммитится)
  └── memory/              # Runtime данные (игнорируется git)
      └── storage.db       # База данных воспоминаний
```

## 🔗 Полезные ссылки

- [MCP Protocol](https://modelcontextprotocol.io/)
- [Memory Server GitHub](https://github.com/modelcontextprotocol/servers/tree/main/src/memory)
- [Cursor MCP Documentation](https://docs.cursor.com/mcp)

## 📝 Следующие шаги

1. ✅ Установить Node.js (если ещё не установлен)
2. ✅ Перезапустить Cursor IDE
3. ✅ Протестировать Memory (сохранить → перезапуск → вспомнить)
4. ✅ Сохранить основную информацию о проекте
5. ✅ Использовать Memory для улучшения работы с проектом

---

**Создано:** 18 января 2026  
**Версия:** 1.0.0  
**Статус:** ✅ Готово к использованию
