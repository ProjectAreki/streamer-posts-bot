# 🎰 Тест названий слотов - проверка фильтра

## Популярные слоты - проверка на конфликты с фильтром

### ✅ БЕЗОПАСНЫЕ (точно работают):

1. **Gates of Olympus** - "gates" не в списке запрещённых фраз
2. **Sugar Rush** - "sugar" не в списке
3. **Sweet Bonanza** - "sweet", "bonanza" не в списке
4. **The Dog House** - "the" только в составе фраз ("the abyss")
5. **Fruit Party** - "fruit", "party" не в списке
6. **Big Bass Bonanza** - "big", "bass" не в списке
7. **Starlight Princess** - "starlight", "princess" не в списке
8. **Book of Dead** - "book", "dead" не в списке
9. **Aztec Magic** - "aztec", "magic" не в списке
10. **Wild West Gold** - "wild", "west", "gold" не в списке (wild был удалён)

### ⚠️ ТРЕБУЮТ ВНИМАНИЯ (содержат слова из фильтра):

1. **Massive Wilds** - содержит "massive" ❌
   - **Защита:** Если "massive" есть в slot_lower → ИГНОРИРУЕТСЯ ✅
   
2. **Power Boost** - содержит "boost" ❌
   - **Защита:** Если "boost" есть в slot_lower → ИГНОРИРУЕТСЯ ✅
   
3. **Activate Megaways** - содержит "activate" ❌
   - **Защита:** Если "activate" есть в slot_lower → ИГНОРИРУЕТСЯ ✅

4. **Stars Aligned** - содержит "aligned" ❌
   - **Защита:** Если "aligned" есть в slot_lower → ИГНОРИРУЕТСЯ ✅

### 📋 Текущий список запрещённых фраз:

```python
english_phrases = [
    'the abyss',           # фраза (2 слова)
    'answered the call',   # фраза (3 слова)
    'summoning circle',    # фраза
    'play it safe',        # фраза
    'bright lights',       # фраза
    'chose to dive',       # фраза
    'deep into',          # фраза
    'dark forces',        # фраза
    'aligned',            # ⚠️ ОДНО СЛОВО (может быть в слотах!)
    'full-blown ritual',  # фраза
    'pulled straight',    # фраза
    'from the void',      # фраза
    'sometimes',          # ⚠️ ОДНО СЛОВО
    'when you stare',     # фраза
    'into the darkness',  # фраза
    'hands you',          # фраза
    'fortune in return',  # фраза
    'outcome is terrifyingly good', # фраза
    'claim the',          # фраза
    'massive',            # ⚠️ ОДНО СЛОВО (может быть в слотах!)
    'boost',              # ⚠️ ОДНО СЛОВО (может быть в слотах!)
    'activate',           # ⚠️ ОДНО СЛОВО (может быть в слотах!)
    'balance power',      # фраза
    'visuals shifted',    # фраза
    'eerie sounds peaked', # фраза
    'screen locked',      # фраза
    'random luck',        # фраза
    'felt like',          # фраза
    'handshake with the supernatural' # фраза
]
```

### 🔒 Механизм защиты:

```python
for phrase in english_phrases:
    if phrase_lower in text_lower:
        # 1. Проверка на допустимые слова (USD, EUR, GBP, FS...)
        if phrase_lower in allowed_words:
            continue
        
        # 2. Проверка - не из названия слота?
        if phrase_lower not in slot_lower:
            found_english.append(phrase)  # Только тогда отвергаем!
```

### ✅ Вывод:

**ВСЕ популярные слоты ЗАЩИЩЕНЫ:**
- Если слово из фильтра есть в названии слота → ИГНОРИРУЕТСЯ
- USD/EUR/GBP явно добавлены в allowed_words → ИГНОРИРУЮТСЯ
- FS (freespins), MAX BET, WIN и другие термины → ИГНОРИРУЮТСЯ

### 🎯 Примеры работы:

#### Пример 1: Слот "2 Wild 2 Die"
```
AI пишет: "стример играл в 2 Wild 2 Die"
Фильтр проверяет: "wild" в тексте? ДА
Фильтр проверяет: "wild" в названии слота "2 wild 2 die"? ДА
РЕШЕНИЕ: ✅ ПРОПУСКАЕМ (не отвергаем)
```

#### Пример 2: Слот "Gates of Olympus", ставка в USD
```
AI пишет: "выигрыш 1000 USD в Gates of Olympus"
Фильтр проверяет: "usd" в тексте? ДА
Фильтр проверяет: "usd" в allowed_words? ДА
РЕШЕНИЕ: ✅ ПРОПУСКАЕМ (не отвергаем)
```

#### Пример 3: Реальный английский текст
```
AI пишет: "the abyss opened and gave fortune"
Фильтр проверяет: "the abyss" в тексте? ДА
Фильтр проверяет: "the abyss" в слоте? НЕТ
РЕШЕНИЕ: ❌ ОТВЕРГАЕМ (это реальный английский текст!)
```

---

**Дата:** 20 января 2026  
**Статус:** ✅ ВСЕ СЛОТЫ ЗАЩИЩЕНЫ
