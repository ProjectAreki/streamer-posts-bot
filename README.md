# 📹 Streamer Posts Bot

Telegram бот для генерации 100 уникальных постов про стримеров казино через AI (OpenRouter).

> ✨ **Полная адаптация завершена!** Бот имеет все необходимые компоненты и полностью совместим с оригинальными handlers. См. [ADAPTATION_COMPLETE.md](./ADAPTATION_COMPLETE.md)

## 🎯 Что делает бот

- **80 постов с видео** - уникальные тексты про выигрыши стримеров
- **20 постов с картинками** - бонусные предложения
- **15+ AI моделей** - через OpenRouter API
- **Проверка уникальности** - "Сторожевой AI" находит дубли
- **Автопубликация** - в Telegram канал с умными паузами

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка конфигурации

```bash
# Скопируйте шаблон конфигурации
cp env.example .env

# Отредактируйте .env и заполните:
nano .env
```

**Обязательные параметры:**

```env
TELEGRAM_BOT_TOKEN=your_token_from_botfather
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=your_hash_from_my_telegram_org
OPENROUTER_API_KEY=sk-or-v1-your_openrouter_key
```

#### Где получить API ключи:

- **TELEGRAM_BOT_TOKEN**: @BotFather в Telegram → `/newbot`
- **TELEGRAM_API_ID + API_HASH**: https://my.telegram.org/apps
- **OPENROUTER_API_KEY**: https://openrouter.ai/ → Settings → API Keys

### 3. Запуск бота

```bash
python bot.py
```

## 📖 Как пользоваться

### Шаг 1: Запуск сценария

1. Отправьте `/start` боту
2. Нажмите **"📹 100 постов стримеров"**

### Шаг 2: Ввод данных

1. **Ссылки и бонусы** (2 штуки):
   - URL 1 + описание бонуса (например: "100 FS")
   - URL 2 + описание бонуса (например: "150% + 500 FS")

2. **Канал для публикации**:
   - Выберите из списка
   - Или введите вручную (@channel или ID)

3. **Видео** (до 80 штук):
   - **Вариант A**: Загрузите вручную через Telegram
   - **Вариант B**: Возьмите из канала (через Telethon)
   
   **Формат имени файла** (если загружаете):
   ```
   Стример_Слот_Ставка_Выигрыш.mp4
   ```
   Пример: `Жека_Gates of Olympus_500_125000.mp4`

4. **Картинки** (опционально, до 20 штук):
   - Загрузите вручную или возьмите из канала

### Шаг 3: Выбор AI модели

**Режимы генерации:**

| Режим | Описание | Стоимость за 100 постов |
|-------|----------|-------------------------|
| 🔄 **Ротация дешёвых** | 6 моделей (~0.05₽/пост) | ~5₽ |
| ⚖️ **Ротация средних** | 4 модели (~0.3₽/пост) | ~30₽ |
| 💎 **Ротация премиум** | 5 моделей (~1₽/пост) | ~100₽ |
| 🤖 **Одна модель** | Выберите конкретную | От 3₽ до 280₽ |

**Доступные модели:**

- 💰 **Дешёвые** (0.03-0.1₽): Grok, Mistral, Llama, DeepSeek, Qwen
- ⚖️ **Средние** (0.2-0.5₽): Gemini Flash, GPT-4.1 Mini
- 💎 **Премиум** (0.8-3₽): GPT-5.2, Claude, Gemini Pro

### Шаг 4: AI генерация

Бот сгенерирует уникальные тексты для каждого поста:
- Разные промпты
- Вариации описаний бонусов
- 15 форматов блоков ссылок

**Время генерации:**
- Дешёвые модели: 3-5 минут
- Средние модели: 5-8 минут
- Премиум модели: 10-15 минут

### Шаг 5: Проверка уникальности

"Сторожевой AI" проверит все посты на дубли:
- Одинаковые начала
- Повторяющиеся фразы
- Похожие структуры
- Шаблонные элементы

**Если найдены дубли:**
- Список похожих постов
- Кнопка "🔄 Перегенерировать"
- Автоматическая перегенерация

### Шаг 6: Превью и публикация

1. Просмотрите случайные посты
2. Нажмите **"✅ Начать публикацию"**
3. Бот опубликует все посты с умными паузами:
   - 3-5 сек между постами
   - 60 сек каждые 20 постов
   - 5 минут каждые 100 постов

**Время публикации:** ~12-15 минут на 100 постов

## 💰 Стоимость использования

### Минимальный бюджет для старта:

**OpenRouter:**
- Минимальное пополнение: $5
- Хватит на: 500-1000 постов (зависит от модели)

### Примеры расчёта:

```
Дешёвая ротация (Qwen, DeepSeek):
100 постов = ~5₽ (~$0.06)

Оптимальная (Gemini Flash):
100 постов = ~40₽ (~$0.45)

Премиум (Claude Opus):
100 постов = ~280₽ (~$3.00)
```

## 📁 Структура проекта

```
streamer_posts_bot/
├── bot.py                          # Главный файл бота (✨ адаптирован)
├── requirements.txt                # Зависимости Python
├── env.example                     # Шаблон конфигурации
├── .gitignore                      # Игнорируемые файлы
├── README.md                       # Эта инструкция
├── ADAPTATION_COMPLETE.md          # ✨ Документация адаптации
├── deploy.sh                       # Скрипт для деплоя на сервер
├── src/
│   ├── __init__.py
│   ├── config.py                   # Конфигурация из .env
│   ├── config_manager.py           # ✨ Менеджер конфигурации
│   ├── logger.py                   # ✨ Система логирования
│   ├── chat_scanner.py             # ✨ Telethon клиент для каналов
│   ├── ai_post_generator.py        # AI генератор (3547 строк)
│   ├── bonus_generator.py          # Генератор вариаций бонусов
│   ├── handlers/
│   │   ├── __init__.py
│   │   └── streamer_posts_handlers.py  # Handlers (без изменений)
│   └── states/
│       └── __init__.py             # FSM состояния
├── logs/                           # Логи бота
└── data/                           # Данные (session файлы)
```

## 🔧 Требования

### Python
- Python 3.9 или выше

### API ключи
- ✅ **TELEGRAM_BOT_TOKEN** - обязательно
- ✅ **TELEGRAM_API_ID** - обязательно
- ✅ **TELEGRAM_API_HASH** - обязательно
- ✅ **OPENROUTER_API_KEY** - обязательно
- ⚠️ **OPENAI_API_KEY** - опционально (fallback)

### Telegram сессия
При первом запуске бот создаст `.session` файл для Telethon:
1. Введите номер телефона
2. Введите код из Telegram
3. Введите пароль 2FA (если есть)

## 🚀 Деплой на сервер

### Вариант 1: Ручной деплой

```bash
# 1. Клонируйте проект на сервер
scp -r streamer_posts_bot user@server:/path/to/

# 2. Подключитесь к серверу
ssh user@server

# 3. Перейдите в директорию
cd /path/to/streamer_posts_bot

# 4. Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 5. Установите зависимости
pip install -r requirements.txt

# 6. Настройте .env
nano .env

# 7. Запустите бота
python bot.py
```

### Вариант 2: Автоматический деплой

```bash
# Используйте скрипт deploy.sh
chmod +x deploy.sh
./deploy.sh
```

### Systemd сервис (Linux)

Создайте `/etc/systemd/system/streamer-bot.service`:

```ini
[Unit]
Description=Streamer Posts Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/streamer_posts_bot
Environment="PATH=/path/to/streamer_posts_bot/venv/bin"
ExecStart=/path/to/streamer_posts_bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable streamer-bot
sudo systemctl start streamer-bot
sudo systemctl status streamer-bot
```

## 📊 Логи

Логи сохраняются в `logs/bot.log`:

```bash
# Просмотр логов в реальном времени
tail -f logs/bot.log

# Последние 100 строк
tail -n 100 logs/bot.log

# Поиск ошибок
grep "ERROR" logs/bot.log
```

## ❓ FAQ

### Бот не запускается?

1. Проверьте `.env` файл - все ключи заполнены?
2. Проверьте `requirements.txt` - все зависимости установлены?
3. Посмотрите логи: `tail -f logs/bot.log`

### OpenRouter возвращает ошибку 401?

- Проверьте `OPENROUTER_API_KEY`
- Проверьте баланс на https://openrouter.ai/

### Telethon не подключается?

- Проверьте `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Удалите `.session` файл и попробуйте снова

### Публикация в канал не работает?

- Бот должен быть **администратором** канала
- У бота должны быть права на публикацию

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи
2. Проверьте конфигурацию
3. Проверьте API ключи
4. Проверьте баланс OpenRouter

## 📝 Лицензия

MIT License

---

**Создано:** 14 января 2026  
**Версия:** 1.0.0  
**Python:** 3.9+
