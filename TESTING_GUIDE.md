# 🧪 Руководство по тестированию StreamerPostsBot

## ✅ Предварительная проверка

### 1. Проверка структуры проекта

```bash
cd streamer_posts_bot
ls -la
```

**Должны быть:**
- ✅ `bot.py`
- ✅ `requirements.txt`
- ✅ `.env.example`
- ✅ `src/` директория
- ✅ `src/config.py`
- ✅ `src/config_manager.py` (новый)
- ✅ `src/logger.py` (новый)
- ✅ `src/chat_scanner.py` (новый)
- ✅ `src/ai_post_generator.py`
- ✅ `src/bonus_generator.py`
- ✅ `src/handlers/streamer_posts_handlers.py`
- ✅ `src/states/__init__.py`

---

## 🚀 Локальное тестирование

### Шаг 1: Установка зависимостей

```bash
# Создаём виртуальное окружение
python -m venv venv

# Активируем (Windows)
venv\Scripts\activate

# Активируем (Linux/Mac)
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

**Проверка:**
```bash
pip list | grep -E "aiogram|telethon|openai|python-dotenv"
```

Должны быть установлены:
- aiogram
- telethon
- openai
- python-dotenv

---

### Шаг 2: Настройка .env файла

```bash
# Копируем шаблон
cp .env.example .env

# Редактируем (Windows)
notepad .env

# Редактируем (Linux/Mac)
nano .env
```

**Минимально необходимые настройки:**

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=1234567890:XXXXXXXXXXXXXXXXXXXXXXXXXXX

# Telegram API (для Telethon)
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890

# OpenRouter (основной)
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI (опционально, fallback)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
```

---

### Шаг 3: Запуск бота

```bash
python bot.py
```

**Ожидаемый вывод:**

```
2026-01-14 12:00:00 | INFO     | StreamerPostsBot | ✅ Бот инициализирован
2026-01-14 12:00:01 | INFO     | StreamerPostsBot | Telethon клиент запущен
2026-01-14 12:00:02 | INFO     | StreamerPostsBot | 🚀 Запуск бота...
```

**Возможные проблемы:**

| Ошибка | Решение |
|--------|---------|
| `ModuleNotFoundError: No module named 'aiogram'` | Установите зависимости: `pip install -r requirements.txt` |
| `FileNotFoundError: [Errno 2] No such file or directory: '.env'` | Создайте `.env` файл из `.env.example` |
| `ValidationError` в config.py | Проверьте формат переменных в `.env` |
| `Unauthorized` от Telegram | Проверьте `TELEGRAM_BOT_TOKEN` |

---

## 🧪 Тест 1: Базовые команды

### 1.1 Команда /start

**Действие:**
1. Откройте бота в Telegram
2. Отправьте `/start`

**Ожидаемый результат:**
- Приветственное сообщение
- Кнопки:
  - "📹 100 постов стримеров"
  - "❓ Помощь"

**Логи:**
```
StreamerPostsBot | Пользователь 123456789 запустил команду /start
```

---

### 1.2 Команда /help

**Действие:**
1. Отправьте `/help` или нажмите "❓ Помощь"

**Ожидаемый результат:**
- Подробная инструкция
- Информация о моделях
- Ссылки на API сервисы

---

## 🧪 Тест 2: Запуск сценария "100 постов стримеров"

### 2.1 Начало сценария

**Действие:**
1. Нажмите кнопку "📹 100 постов стримеров"

**Ожидаемый результат:**
- Информационное сообщение о сценарии
- Кнопки:
  - "🚀 Начать настройку"
  - "📖 Как именовать файлы"
  - "❌ Отмена"

**Проверяемые компоненты:**
- ✅ `bot_instance.is_scenario_allowed()` работает
- ✅ Handler зарегистрирован корректно
- ✅ FSM состояние изменяется

---

### 2.2 Ввод ссылок и бонусов

**Действие:**
1. Нажмите "🚀 Начать настройку"
2. Введите первую ссылку: `https://example.com/bonus1`
3. Введите первый бонус: `100 FS + 150%`
4. Введите вторую ссылку: `https://example.com/bonus2`
5. Введите второй бонус: `200 FS + 200%`

**Ожидаемый результат:**
- Бот принимает каждый ввод
- Переходит к следующему шагу
- Сохраняет данные в FSM state

**Логи:**
```
StreamerPostsBot | Пользователь 123456789 ввёл ссылки и бонусы
```

---

### 2.3 Выбор канала для публикации

**Действие:**
1. Выберите "📢 Мои каналы"

**Ожидаемый результат:**
- Telethon клиент подключается
- Показывается список ваших каналов
- Кнопки с названиями каналов

**Проверяемые компоненты:**
- ✅ `chat_scanner.start()` успешен
- ✅ `chat_scanner.get_user_channels()` работает
- ✅ `show_user_channels()` метод функционирует

**Возможные проблемы:**

| Проблема | Решение |
|----------|---------|
| Telethon просит номер телефона | Введите номер, затем код из Telegram |
| "Каналы не найдены" | Убедитесь, что вы создатель/админ хотя бы одного канала |
| Session ошибка | Удалите `.session` файлы и попробуйте снова |

---

## 🧪 Тест 3: Проверка компонентов

### 3.1 ConfigManager

**Код для проверки:**

```python
from src.config import Config
from src.config_manager import ConfigManager

config = Config.from_env()
config_manager = ConfigManager.from_config(config)

print("✅ ConfigManager создан")
print(f"AI Provider: {config_manager.default_ai_provider}")
print(f"AI Model: {config_manager.default_model}")
print(f"OpenRouter Key: {config_manager.openrouter_api_key[:20]}...")
```

**Ожидаемый вывод:**
```
✅ ConfigManager создан
AI Provider: openrouter
AI Model: deepseek/deepseek-chat
OpenRouter Key: sk-or-v1-xxxxxxxxxx...
```

---

### 3.2 BotLogger

**Код для проверки:**

```python
from src.logger import BotLogger

logger = BotLogger()

logger.info("Тест INFO", user_id=123)
logger.warning("Тест WARNING", action="test")
logger.error("Тест ERROR", code=500)

print("✅ Логгер работает, проверьте logs/bot_YYYYMMDD.log")
```

**Проверка:**
```bash
cat logs/bot_*.log | tail -3
```

**Ожидаемый вывод:**
```
2026-01-14 12:00:00 | INFO     | StreamerPostsBot | Тест INFO | user_id=123
2026-01-14 12:00:01 | WARNING  | StreamerPostsBot | Тест WARNING | action=test
2026-01-14 12:00:02 | ERROR    | StreamerPostsBot | Тест ERROR | code=500
```

---

### 3.3 ChatScanner

**Код для проверки:**

```python
import asyncio
from src.config import Config
from src.logger import BotLogger
from src.chat_scanner import ChatScanner

async def test_scanner():
    config = Config.from_env()
    logger = BotLogger()
    
    scanner = ChatScanner(
        api_id=config.telegram.api_id,
        api_hash=config.telegram.api_hash,
        logger=logger
    )
    
    # Запуск
    result = await scanner.start()
    print(f"✅ Telethon запущен: {result}")
    
    # Получение каналов
    channels = await scanner.get_user_channels()
    print(f"✅ Найдено каналов: {len(channels)}")
    
    for ch in channels:
        print(f"  - {ch['title']} (ID: {ch['id']})")
    
    # Остановка
    await scanner.stop()
    print("✅ Telethon остановлен")

asyncio.run(test_scanner())
```

**Ожидаемый вывод:**
```
✅ Telethon запущен: True
✅ Найдено каналов: 3
  - Мой канал 1 (ID: -1001234567890)
  - Тестовый канал (ID: -1009876543210)
  - Gaming News (ID: -1001111222333)
✅ Telethon остановлен
```

---

## 🧪 Тест 4: Интеграция AI генератора

**Действие:**
1. Продолжите сценарий до выбора AI модели
2. Выберите любую модель (рекомендую дешёвую для теста)
3. Загрузите 1-2 тестовых видео
4. Запустите генерацию

**Ожидаемый результат:**
- AI генератор получает данные
- OpenRouter API вызывается успешно
- Генерируются уникальные тексты

**Проверяемые компоненты:**
- ✅ `ai_post_generator.py` работает с `config_manager`
- ✅ OpenRouter API ключ валиден
- ✅ Генерация текстов проходит без ошибок

**Логи:**
```
StreamerPostsBot | Начата генерация | posts=2, model=deepseek/deepseek-chat
StreamerPostsBot | Пост 1/2 сгенерирован | model=deepseek/deepseek-chat
StreamerPostsBot | Пост 2/2 сгенерирован | model=deepseek/deepseek-chat
StreamerPostsBot | Генерация завершена | total=2, time=3.5s
```

---

## ✅ Чеклист полной проверки

- [ ] Бот запускается без ошибок
- [ ] Логи создаются в `logs/`
- [ ] `/start` показывает меню
- [ ] `/help` показывает инструкцию
- [ ] Кнопка "📹 100 постов стримеров" работает
- [ ] Ввод ссылок и бонусов принимается
- [ ] Telethon подключается и показывает каналы
- [ ] ConfigManager инициализируется
- [ ] BotLogger пишет логи
- [ ] ChatScanner получает список каналов
- [ ] AI генератор создаёт тексты (если есть API ключ)

---

## 🐛 Отладка

### Включение DEBUG логов

В `bot.py` измените:

```python
# Было:
self.logger = BotLogger()

# Стало:
self.logger = BotLogger()
self.logger.logger.setLevel(logging.DEBUG)
```

### Проверка переменных окружения

```bash
# Windows
echo %TELEGRAM_BOT_TOKEN%

# Linux/Mac
echo $TELEGRAM_BOT_TOKEN
```

### Проверка импортов

```python
# Запустите в Python REPL
from src.config import Config
from src.config_manager import ConfigManager
from src.logger import BotLogger
from src.chat_scanner import ChatScanner
from src.handlers.streamer_posts_handlers import register_streamer_handlers

print("✅ Все импорты успешны!")
```

---

## 📊 Метрики успешного теста

| Критерий | Цель | Статус |
|----------|------|--------|
| Запуск бота | 0 ошибок | ⏳ |
| Базовые команды | 100% работают | ⏳ |
| Telethon подключение | Успешно | ⏳ |
| Получение каналов | Список не пуст | ⏳ |
| AI генерация (опц.) | Успешно | ⏳ |
| Логирование | Файлы создаются | ⏳ |

---

## 🎯 Следующие шаги

После успешного локального тестирования:

1. **Тест полного цикла** - генерация и публикация 5-10 постов
2. **Нагрузочный тест** - генерация 100 постов
3. **Деплой на сервер** - используйте `deploy.sh`
4. **Production мониторинг** - настройте systemd и логи

---

**Удачи в тестировании! 🚀**
